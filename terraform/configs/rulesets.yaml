# Organization rulesets are managed manually in org settings until the
# GitHub Terraform provider fixes the crash in github_organization_ruleset.
# The provider v6.12.0 crashes when creating rulesets with App tokens.
#
# Current rulesets (managed manually):
#   - require-pr: require PR for all repos on default branch
#   - require-pr-validation: require PR Validation check to pass
#   - block-force-push: non_fast_forward on default branch
#
# Rulesets will be imported to Terraform when the provider is stable.

rulesets: []

