<!-- TEMPLATE: Populate this file with project-specific technical conventions -->

# Conventions

<!-- AGENT: Populate each section below by inspecting the repository contents, configuration files, and pinned context issue. Remove agent comments after populating each section. -->

## Technology stack

<!-- AGENT: List the languages, frameworks, runtimes, and tools used in this project. Check package manifests (package.json, requirements.txt, go.mod, etc.) and configuration files to identify the stack. Remove this comment after populating. -->

## Configuration

<!-- AGENT: Describe how the project is configured: environment variables, config files, secrets. List required variables and where they are set (local .env, CI secrets, etc.). Remove this comment after populating. -->

## Known gotchas

<!-- AGENT: Document any non-obvious behaviors, footguns, or constraints a contributor should know before touching this codebase. Add entries as you discover them. Remove this comment after initial population (but keep the section for future entries). -->
